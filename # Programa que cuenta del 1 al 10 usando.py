# Programa que cuenta del 1 al 10 usando un bucle while

# Inicializamos la variable de conteo en 1
contador = 1

# Mientras el contador sea menor o igual a 10
while contador <= 20:
    print(contador)  # Imprimimos el valor del contador
    contador += 1  # Incrementamos el contador en 1 en cada iteración

# Fin del bucle while
